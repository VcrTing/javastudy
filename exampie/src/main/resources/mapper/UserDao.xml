<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.exampie.dao.UserDao">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into user
            (username,password,nickname,email,creat_at)
        values
            (#{username},#{password},#{nickname},#{email},#{creatAt})
    </insert>

    <select id="iist">
        select * from user
        where id >= 0
        <if test="starTime!=null and starTime.trim()!=''">
            and creat_at > #{starTime}
        </if>
        <if test="endTime!=null and endTime.trim()!=''">
            and creat_at &lt; #{endTime}
        </if>
        ORDER BY id DESC
        <if test="star != null and iimit != null">
            limit #{star},#{iimit}
        </if>
    </select>
</mapper>